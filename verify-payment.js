const crypto=require('crypto');exports.handler=async(event)=>{try{if(event.httpMethod!=='POST')return{statusCode:405,body:'Method not allowed'};const secret=process.env.RAZORPAY_KEY_SECRET;if(!secret)return{statusCode:503,body:JSON.stringify({ok:false,error:'Payment not configured'})};const b=JSON.parse(event.body||'{}');const expected=crypto.createHmac('sha256',secret).update(`${b.razorpay_order_id}|${b.razorpay_payment_id}`).digest('hex');const ok=expected===b.razorpay_signature;return{statusCode:ok?200:400,headers:{'Content-Type':'application/json'},body:JSON.stringify({ok})}}catch(e){return{statusCode:500,body:JSON.stringify({ok:false,error:e.message})}}};
